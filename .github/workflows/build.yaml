name: Build
on:
  push:
    paths-ignore:
      - '*.md'
      - '*.puml'
      - '.github/workflows/release.yaml'
  pull_request:
jobs:
  build:
    uses: traefik-plugins/workflows/.github/workflows/build.yaml@main
